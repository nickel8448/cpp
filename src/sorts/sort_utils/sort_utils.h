#ifndef SORTUTILS
#define SORTUTILS

#include <vector>

namespace SortUtil {
    
    void swap(int* xp, int* yp);

    void printArray(const int arr[], const int size);

    void printVector(const std::vector<int>& elemts);
}

#endif
